/*#include "ABB.h"

int main()
{
    conjunto a, b, c;
    arbolConjuntos arbol;
    crearABB(arbol);
    crearConjunto(a,"tercero");
    agregarElemento(a,3);
    agregarElemento(a,1);
    agregarConjunto(arbol, a);
    crearConjunto(a, "primero");
    agregarElemento(a,7);
    agregarElemento(a,8);
    agregarConjunto(arbol, a);
    crearConjunto(a, "segundo");
    agregarConjunto(arbol, a);
    mostrarABB(arbol);
    agregarElementoABB(arbol, "segundo", 9);
    mostrarABB(arbol);
    agregarElementoABB(arbol, "primero", 15);
    mostrarABB(arbol);
    crearConjunto(a, "blep");
    crearConjunto(b, "uwu");
    crearConjunto(c, "union");
    a = devolverConjunto(arbol, "primero");
    b = devolverConjunto(arbol, "segundo");
    unionConjunto(a, b, c);
    agregarConjunto(arbol, c);
    mostrarABB(arbol);


   return 0;
}
*/
