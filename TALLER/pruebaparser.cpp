/*
#include "parser.h"

int main()
{
   parser par;
   crearParser(par);
   cargarParser(par,"guardar");
   mostrarParser(par);
   desplegarComando(asignComando(par));

    if (validarGuardar(par))
    {
        printf("\nTRUE");

    }
    else
        printf("\nFALSE");


   return 0;
}

*/
